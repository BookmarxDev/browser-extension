import { DirectoryMenuAction } from './directory-menu-action';

describe('DirectoryMenuAction', () => {
  it('should create an instance', () => {
    expect(new DirectoryMenuAction()).toBeTruthy();
  });
});
